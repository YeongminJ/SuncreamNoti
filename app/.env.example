# 서버(suncream-noti-api) 베이스 URL.
# 로컬 dev: http://localhost:8787 (wrangler dev 기본)
# 프로덕션: https://suncream-noti-api.<your-cf-account>.workers.dev
#
# 이 파일을 .env.local로 복사하고 값을 채워서 사용하세요.
# .env.local은 .gitignore로 제외돼요.

VITE_API_URL=http://localhost:8787
